---
import "../css/global.css";
import "../css/reset.css";
---

<section>
  <div class="textbox">
    <h2>
      “Jeg er en <mark>nysgerrig og engageret</mark> studerende med interesse for
      <mark>webudvikling samt design</mark>. Jeg ønsker og <mark>udvikle</mark> mine
      evner indenfor bæredygtig webudvikling, og være med til at skabe <mark
        >holdbare løsninger</mark
      > for fremtiden”
    </h2>
  </div>
</section>

<style>
  mark {
    background: linear-gradient(
      to right,
      var(--primary-color-01) 50%,
      var(--secondary-color-01) 50%
    );
    background-position: 100%;
    background-size: 200% 100%;
    animation: --highlight linear forwards;
    animation-timeline: view(50% 20%);
  }

  @keyframes --highlight {
    to {
      background-position: 0;
    }
  }
  @media (min-width: 481px) {
    section {
      background-color: var(--secondary-color-01);
      padding-top: 10rem;
      padding-bottom: 10rem;
      display: grid;
      grid-template-columns: 1fr 2fr 1fr;

      .textbox {
        border-left: solid 2px var(--primary-color-03);
        padding: 0;
        grid-column: 2/-2;
        h2 {
          color: var(--primary-color-03);
          padding: 1rem;
        }
      }
    }
  }

  @media (max-width: 480px) {
    section {
      background-color: var(--secondary-color-01);
      padding-top: 3rem;
      padding-bottom: 3rem;
      display: grid;
      grid-template-columns: 1fr 2fr 1fr;

      .textbox {
        border-left: solid 2px var(--primary-color-03);
        padding: 0;
        grid-column: 2/-2;
        h2 {
          color: var(--primary-color-03);
          padding: 1rem;
        }
      }
    }
  }
</style>
